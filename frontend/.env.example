# Copy to .env.local (gitignored) for local overrides. Not needed for normal local
# dev — defaults to http://localhost:8000. Required in Vercel's project settings for
# the deployed build, pointed at the live Render backend URL.
VITE_API_BASE_URL=
