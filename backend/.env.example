# Copy to .env (gitignored) and fill in real values. Never commit the real .env.
GEMINI_API_KEY=your_key_here

# Comma-separated list of extra allowed CORS origins (the deployed Vercel frontend
# URL, once known). Local dev origins are always allowed regardless of this value.
FRONTEND_ORIGIN=
